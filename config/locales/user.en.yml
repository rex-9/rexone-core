en:
  user:
    current_fetched: "Current user fetched successfully."
    not_authenticated: "User not authenticated."
    current_not_found: "No current user found."
    iam_fetched: "User IAM fetched"
    current_updated: "Profile updated successfully."
    current_update_failed: "Failed to update profile."
  activerecord:
    attributes:
      user:
        email: "Email"
        username: "Username"
        name: "Name"
        password: "Password"
        password_confirmation: "Password confirmation"
    errors:
      models:
        user:
          attributes:
            username:
              invalid_format: "can only contain lowercase letters, numbers, and underscores"
            confirmation_code:
              invalid_or_expired: "is invalid or has expired"
  errors:
    format: "%{attribute} %{message}"
    messages:
      blank: "can't be blank"
      taken: "has already been taken"
      too_short: "is too short (minimum is %{count} characters)"
      too_long: "is too long (maximum is %{count} characters)"
      wrong_length: "is the wrong length (should be %{count} characters)"
      invalid: "is invalid"
      confirmation: "doesn't match %{attribute}"
